name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  macos:
    name: macOS
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - name: Select a Swift 6 toolchain
        # macos-14 defaults to an Xcode with Swift 5.10; the package needs 6.0.
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable
      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
      - name: Start WireMock and wait for readiness
        # Version pin + readiness gate live in Scripts/start-wiremock.sh, shared
        # with the ios-xcuitest job and local Scripts/test.sh.
        run: Scripts/start-wiremock.sh
      - name: Build
        run: swift build -Xswiftc -strict-concurrency=complete
      - name: Test (unit + integration) with coverage
        env:
          # Fail (don't skip) the integration suites if the server isn't reachable.
          WIREMOCK_REQUIRED: "1"
        run: swift test --enable-code-coverage
      - name: Export code coverage (lcov)
        run: |
          BIN=$(swift build --show-bin-path)
          XCTEST=$(find "$BIN" -name '*.xctest' | head -1)
          PROF=$(find .build -name default.profdata | head -1)
          xcrun llvm-cov export -format=lcov \
            -instr-profile "$PROF" \
            "$XCTEST/Contents/MacOS/$(basename "$XCTEST" .xctest)" \
            > coverage.lcov
          echo "coverage.lcov: $(grep -c '^SF:' coverage.lcov) files, $(wc -l < coverage.lcov) lines"
      - name: Upload coverage artifact
        uses: actions/upload-artifact@v4
        with:
          name: coverage-lcov
          path: coverage.lcov

  lint:
    name: SwiftLint (non-blocking)
    runs-on: macos-14
    # Non-blocking until the team curates a baseline; see .swiftlint.yml. Flip to
    # blocking by removing this line (and add `--strict` below) once clean.
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - name: Install SwiftLint
        # Not preinstalled on current macOS runner images.
        run: brew install swiftlint
      - name: Run SwiftLint
        run: swiftlint lint --reporter github-actions-logging

  ios-build:
    name: iOS build
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - name: Select a Swift 6 toolchain
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable
      - name: Build the library for the iOS Simulator SDK
        # Proves the client compiles and links for iOS.
        run: xcodebuild build -scheme WireMockSwift -destination 'generic/platform=iOS Simulator'

  ios-xcuitest:
    name: iOS XCUITest example
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
      - name: Start WireMock on the host
        run: Scripts/start-wiremock.sh
      - name: Generate and run the XCUITest example on a simulator
        working-directory: Examples/WireMockXCUIDemo
        run: |
          brew install xcodegen
          xcodegen generate
          # Target an available iPhone simulator by UDID (robust — no name pinning).
          UDID=$(xcrun simctl list devices available | grep "iPhone" | grep -oE '[0-9A-F-]{36}' | head -1)
          echo "Using simulator UDID: $UDID"
          # TEST_RUNNER_* env vars are forwarded to the XCUITest runner process
          # (prefix stripped), so the test sees WIREMOCK_REQUIRED=1 and FAILS —
          # rather than XCTSkip-ing green — if it can't reach the server.
          TEST_RUNNER_WIREMOCK_REQUIRED=1 \
          xcodebuild test -project WireMockXCUIDemo.xcodeproj -scheme SampleApp \
            -destination "id=$UDID"
