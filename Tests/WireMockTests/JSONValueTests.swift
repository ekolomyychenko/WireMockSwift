import XCTest
@testable import WireMock

// TEMPORARY diagnostic stub: JSONValue precision changes reverted to isolate a
// CI-only signal-5 crash. Restored (with the precision suite) once the crash
// source is confirmed.
final class JSONValueTests: XCTestCase {}
